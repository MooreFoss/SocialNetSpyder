doctype html
html(lang="zh")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title= title
    style.
      body {
        margin: 0;
        padding: 0;
        height: 100vh;
        overflow: hidden;
      }
      iframe {
        width: 100%;
        height: 100%;
        border: none;
      }
  body
    iframe(src=url, sandbox="allow-scripts allow-same-origin allow-forms" loading="lazy")